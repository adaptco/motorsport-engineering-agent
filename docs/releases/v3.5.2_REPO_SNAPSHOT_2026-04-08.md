# MEA v3.5.2 Snapshot (2026-04-08)

## Snapshot Intent

Freeze a repository-grounded reference point for the `v3.5.2` baseline before parallel `v3.6` closeout work continues.

## Baseline References

- Release tag: `v3.5.2`
- Tag commit: `33521e7bdb34e2704cb5b62ac81c58291cae682b`
- Snapshot date: `2026-04-08`
- Snapshot branch context: `codex/docker`

## Runtime/Version Baseline

- Kernel version: `3.5.2` (`VERSION.json`)
- Package version: `0.3.5.2` (`VERSION.json`, `pyproject.toml`)
- PRD baseline path: `PRD.md`
- MCP PRD path: `mcp_v1_runtime_bundle/PRD.md`

## Key Grounded State at Snapshot Time

- A2A workflow state persistence is present in `worker/background_workers.py`.
- Circuit breakers and DB pool health surfaces are wired (`shared/circuit_breaker.py`, `shared/db.py`, `GET /healthz/dependencies`).
- Task ledger remains the canonical release tracker (`TASK_LEDGER.md`).
- Dependency lock strategy is now grounded on `uv.lock`; stale root `requirements.txt` has been removed.

## Remaining Open V3.6 Preparation Items

- Project-wide lint/type policy completion (`ruff` + `mypy` enforcement path).
- Docker image standardization and full deployment shape reconciliation.
- Strict production fallback policy (`QUEUE_ALLOW_IN_MEMORY_FALLBACK=false` in production profiles).
- End-to-end ingest lifecycle integration coverage.

## Notes

This snapshot is intentionally additive and non-destructive. It records evidence and open gates without rewriting release sequencing ownership defined in `PRD.md`.
