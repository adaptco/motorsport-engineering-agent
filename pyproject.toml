[project]
name = "mea-root-kernel"
version = "0.3.6.3"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.46.0",
  "python-multipart>=0.0.26",
  "pydantic>=2.13.3",
  "jsonschema>=4.23.0",
  "referencing>=0.37.0",
  "psycopg[binary]>=3.3.3",
  "redis>=7.4.0",
  "requests>=2.32.0",
  "PyJWT>=2.12.1",
  "cryptography>=47.0.0",
  "typer>=0.12.0",
  "PyYAML>=6.0.3",
  "pandas>=2.2.0",
  "scipy>=1.17.1",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.3.0",
  "pytest-cov>=5.0.0",
  "httpx>=0.27.0",
  "ruff>=0.15.12",
  "mypy>=1.11.0",
]

[tool.setuptools]
py-modules = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
target-version = "py311"
line-length = 100
extend-exclude = [".venv", ".mea_tmp", "runtime_logs"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = false
check_untyped_defs = true
no_implicit_optional = true
exclude = "(^\\.venv/|^\\.mea_tmp/|^runtime_logs/)"
