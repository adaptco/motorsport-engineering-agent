export function trackMap(container,points=[]){container.innerHTML='<canvas class="chart-canvas"></canvas>';const canvas=container.firstElementChild,draw=()=>{const r=container.getBoundingClientRect(),d=devicePixelRatio||1;canvas.width=r.width*d;canvas.height=r.height*d;const c=canvas.getContext("2d");c.setTransform(d,0,0,d,0,0);c.fillStyle="#080a0e";c.fillRect(0,0,r.width,r.height);if(!points.length){c.fillStyle="#69717c";c.font="11px JetBrains Mono";c.fillText("POSITION DATA UNAVAILABLE",12,20);return}c.strokeStyle="#58d4e8";c.lineWidth=2;c.beginPath();points.forEach((p,i)=>i?c.lineTo(p.x*r.width,p.y*r.height):c.moveTo(p.x*r.width,p.y*r.height));c.stroke()};draw();const ro=new ResizeObserver(draw);ro.observe(container);return{destroy:()=>ro.disconnect()}}