import {statusPill} from "./status-pill.js";import {esc} from "../lib/utils.js";export function recommendationCard(r){return `<div class="rec-card">${statusPill(r.priority||"INFO")}<div class="rec-action">${esc(r.action||"No action specified")}</div><div class="small">${esc(r.trigger||"")} · ${esc(r.evidence_packet_id||"")}</div>${r.expected_effect?`<div class="list-meta">Expected: ${esc(r.expected_effect)}</div>`:""}</div>`}