import {statusPill} from "./status-pill.js";import {esc} from "../lib/utils.js";export function evidenceFeed(items=[]){return items.length?items.map(e=>`<div class="evidence-item"><div>${statusPill(e.severity)}</div><div><div class="list-title">${esc(e.title||e.evidence_packet_id||"Evidence packet")}</div><div class="list-meta">${esc(e.meta||e.session_id||"")} · ${esc(e.source||"backend")}</div></div><span class="mono">${esc(e.time||"now")}</span></div>`).join(""):`<div class="empty">No evidence packets available.</div>`}