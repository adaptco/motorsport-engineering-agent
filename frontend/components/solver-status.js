import {esc,formatNumber} from "../lib/utils.js";export function solverStatus(s={}){const m=s.metric_snapshot||{},e=s.solver_state||{};return `<div class="stack"><div><span class="tag">${esc(e.openfoam_version||"OpenFOAM")}</span> <span class="tag">${esc(e.status||"unknown")}</span></div><div class="split"><div><div class="metric-label">SOLVER</div><div class="data-value">${esc(e.solver_status||"—")}</div></div><div><div class="metric-label">CONFIDENCE</div><div class="data-value">${formatNumber(m.confidence??0,2)}</div></div></div></div>`}