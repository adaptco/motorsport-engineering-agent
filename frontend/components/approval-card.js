import {esc} from "../lib/utils.js";export function approvalCard(item){const id=esc(item.id||item.task_id||"approval");return `<div class="rec-card"><div class="severity-badge severity-critical">HITL REQUIRED</div><div class="rec-action">${esc(item.title||"Pending decision")}</div><div class="small">${esc(item.agent||"agent")} · ${esc(item.reason||"review required")}</div><div class="actions"><button class="button button--danger" data-approval="reject" data-id="${id}">REJECT</button><button class="button" data-approval="diff" data-id="${id}">DIFF</button><button class="button button--primary" data-approval="approve" data-id="${id}">APPROVE</button></div></div>`}