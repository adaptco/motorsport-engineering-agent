import {statusPill} from "./status-pill.js";import {esc} from "../lib/utils.js";export function agentTable(agents={}){const rows=Object.values(agents);return `<div class="table-wrap"><table class="data-table"><thead><tr><th>AGENT</th><th>RUNTIME</th><th>BRANCH</th><th>TASK</th><th>STATE</th><th>LAST</th></tr></thead><tbody>${rows.map(a=>`<tr><td>${esc(a.display_name)}</td><td>${esc(a.runtime)}</td><td class="mono">${esc(a.branch)}</td><td class="mono">—</td><td>${statusPill(a.state||"running")}</td><td class="mono">${esc(a.last_seen_at||"—")}</td></tr>`).join("")||`<tr><td colspan="6" class="empty">No agents reported.</td></tr>`}</tbody></table></div>`}