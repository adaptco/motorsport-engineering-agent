import {get,post} from "../lib/api.js";import {mountBranchScatter} from "../components/branch-scatter.js";import {solverStatus} from "../components/solver-status.js";import {esc,formatNumber} from "../lib/utils.js";
export async function render(m){m.innerHTML=`<div class="page-header"><div><h1 class="page-title">AERO SIMULATION STUDIO</h1><div class="page-subtitle">Simulation runs, branch deltas and solver provenance.</div></div><button class="button button--primary" id="new-run">⊕ NEW RUN</button></div><div class="split"><section class="panel"><div class="panel-header"><span class="panel-title">SIMULATION RUNS</span></div><div id="aero-runs" class="list"></div></section><section class="panel"><div class="panel-header"><span class="panel-title">BRANCH COMPARISON · CL vs CD</span></div><div class="panel-body"><div id="aero-scatter" class="chart-container"></div></div></section></div><div class="split" style="margin-top:12px"><section class="panel"><div class="panel-header"><span class="panel-title">SOLVER STATE</span></div><div class="panel-body" id="solver"></div></section><section class="panel"><div class="panel-header"><span class="panel-title">BRANCH DETAIL</span></div><div class="panel-body" id="branch-detail"><div class="empty">Select a run.</div></div></section></div><div id="run-modal" hidden></div>`;let chart=null;const load=async()=>{try{const runs=await get("/aero/runs");document.getElementById("aero-runs").innerHTML=runs.map(r=>`<button class="list-row" data-run="${esc(r.simulation_run_id)}" style="text-align:left;background:none;color:inherit;border:0"><div class="list-main"><div class="list-title">${esc(r.vehicle_program_id||r.simulation_run_id)}</div><div class="list-meta">${esc(r.lifecycle_state)} · ${esc(r.updated_at||"")}</div></div><span class="tag">${esc(r.simulation_run_id.slice(0,8))}</span></button>`).join("")||'<div class="empty">No simulation runs.</div>'}catch(e){document.getElementById("aero-runs").innerHTML=`<div class="empty">${esc(e.message)}</div>`}};const select=async id=>{try{const s=await get(`/aero/runs/${encodeURIComponent(id)}`);document.getElementById("solver").innerHTML=solverStatus(s);const pts=(s.branches||[]).map((b,i)=>({x:Number(b.expected_delta_cd??0),y:Number(b.expected_delta_cl??0),label:b.branch_name||`B${i+1}`}));chart?.destroy?.();chart=mountBranchScatter(document.getElementById("aero-scatter"),pts);const b=s.branches?.[0];document.getElementById("branch-detail").innerHTML=b?`<div class="stack"><span class="tag">${esc(b.branch_name)}</span><div class="split"><div><div class="metric-label">ΔCL</div><div class="metric-value">${formatNumber(b.expected_delta_cl,3)}</div></div><div><div class="metric-label">ΔCD</div><div class="metric-value">${formatNumber(b.expected_delta_cd,3)}</div></div></div><div class="small">${esc(b.change_summary||"")}</div></div>`:'<div class="empty">No branches yet.</div>'}catch(e){document.getElementById("branch-detail").innerHTML=`<div class="notice">${esc(e.message)}</div>`}};document.getElementById("aero-runs").addEventListener("click",e=>{const b=e.target.closest("[data-run]");if(b)select(b.dataset.run)});document.getElementById("new-run").onclick=()=>{const modal=document.getElementById("run-modal");modal.hidden=false;modal.innerHTML=`<div class="panel" style="margin-top:12px"><div class="panel-header"><span class="panel-title">NEW SIMULATION RUN</span></div><div class="panel-body"><form id="new-run-form" class="form-grid"><div class="field"><label>PROJECT</label><input name="project" value="mea-demo" required></div><div class="field"><label>PROGRAM</label><input name="program" value="gt4" required></div><div class="field"><label>MAKE</label><input name="make" value="Aston Martin" required></div><div class="field"><label>MODEL</label><input name="model" value="GT4" required></div><div class="field"><label>YEAR</label><input name="year" type="number" value="2026"></div><div class="field"><label>CLASS</label><input name="class" value="GT4"></div><div class="field full"><label>OBJECTIVE</label><textarea name="objective" required>Baseline aerodynamic correlation and branch comparison</textarea></div><div class="field full"><label>GEOMETRY STRATEGY</label><select name="strategy"><option>proxy_geometry</option><option>public_cad</option><option>imported_cad</option><option>manual_sketch</option></select></div><div class="actions full"><button type="button" class="button" id="close-run">CANCEL</button><button class="button button--primary">CREATE RUN</button></div></form></div></div>`;document.getElementById("close-run").onclick=()=>modal.hidden=true;document.getElementById("new-run-form").onsubmit=async e=>{e.preventDefault();const f=new FormData(e.target);try{await post("/aero/runs",{project_id:f.get("project"),vehicle_program_id:f.get("program"),vehicle_identity:{make:f.get("make"),model:f.get("model"),year:Number(f.get("year"))||null,vehicle_class:f.get("class")},source_refs:[],simulation_objective:f.get("objective"),baseline_geometry_strategy:f.get("strategy"),metadata:{ui:"mea"}});modal.hidden=true;await load()}catch(err){modal.querySelector(".panel-body").insertAdjacentHTML("afterbegin",`<div class="notice">${esc(err.message)}</div>`)}}};await load()}