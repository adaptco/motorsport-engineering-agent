export function createRequestId(prefix="req"){return `${prefix}_${crypto.randomUUID?.()||Date.now().toString(36)}`}export function clamp(v,min,max){return Math.min(max,Math.max(min,v))}export function formatNumber(v,d=2){if(v===null||v===undefined||Number.isNaN(Number(v)))return "—";return Number(v).toLocaleString(undefined,{maximumFractionDigits:d})}export function formatDuration(ms){if(ms<1000)return `${Math.round(ms)}ms`;const s=Math.floor(ms/1000);return `${Math.floor(s/60)}m ${s%60}s`}export function debounce(fn,wait){let t;return(...a)=>{clearTimeout(t);t=setTimeout(()=>fn(...a),wait)}}export function throttle(fn,wait){let ready=true;return(...a)=>{if(!ready)return;ready=false;fn(...a);setTimeout(()=>ready=true,wait)}}export function safeJsonParse(v,fallback=null){try{return JSON.parse(v)}catch{return fallback}}export function assert(condition,message="Assertion failed"){if(!condition)throw new Error(message)}export function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]))}