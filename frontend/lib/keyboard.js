import {emit} from "./events.js";const shortcuts=new Map();let enabled=true;function key(e){return `${e.ctrlKey?"Ctrl+":""}${e.altKey?"Alt+":""}${e.shiftKey?"Shift+":""}${e.key}`};export function registerShortcut(combo,fn){shortcuts.set(combo,fn);return()=>shortcuts.delete(combo)}export function unregisterShortcut(combo){shortcuts.delete(combo)}export function enableKeyboard(){enabled=true}export function disableKeyboard(){enabled=false}export function initKeyboard(){document.addEventListener("keydown",e=>{if(!enabled)return;const el=e.target;const typing=el instanceof HTMLInputElement||el instanceof HTMLTextAreaElement||el instanceof HTMLSelectElement||el?.isContentEditable;if(typing&&!e.ctrlKey)return;const fn=shortcuts.get(key(e));if(fn){e.preventDefault();fn(e);emit("keyboard:shortcut",{combo:key(e)})}})}