apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: mea
  labels:
    app: mcp-server
spec:
  type: ClusterIP
  ports:
  - port: 7000
    targetPort: 7000
    protocol: TCP
    name: http
  selector:
    app: mcp-server
